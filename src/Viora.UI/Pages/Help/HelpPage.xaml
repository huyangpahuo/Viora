<UserControl
    x:Class="Viora.UI.Pages.Help.HelpPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:loc="clr-namespace:Viora.UI.Localization"
    xmlns:pages="clr-namespace:Viora.UI.Pages">
    <pages:PageChrome TitleKey="Help.Title" SubtitleKey="Help.Subtitle">
        <StackPanel MaxWidth="720">
            <Border Style="{DynamicResource VioraCard}" Margin="0,8,0,16">
                <StackPanel>
                    <TextBlock Style="{DynamicResource Typography.Heading}" FontSize="15" Text="{loc:Translate Help.GettingStarted.Title}" />
                    <TextBlock Style="{DynamicResource Typography.BodySecondary}" Text="{loc:Translate Help.GettingStarted.Body}" TextWrapping="Wrap" Margin="0,6,0,0" />
                </StackPanel>
            </Border>
            <UniformGrid Columns="2">
                <Button Style="{DynamicResource SecondaryButton}" Content="{loc:Translate Help.Docs}" Command="{Binding OpenDocsCommand}" Margin="0,0,12,12" Height="44" />
                <Button Style="{DynamicResource SecondaryButton}" Content="{loc:Translate Help.Faq}" Command="{Binding OpenFaqCommand}" Margin="0,0,0,12" Height="44" />
                <Button Style="{DynamicResource SecondaryButton}" Content="{loc:Translate Help.GitHubDocs}" Command="{Binding OpenWikiCommand}" Margin="0,0,12,0" Height="44" />
                <Button Style="{DynamicResource SecondaryButton}" Content="{loc:Translate Help.OfficialSite}" Command="{Binding OpenSiteCommand}" Margin="0,0,0,0" Height="44" />
            </UniformGrid>
            <TextBlock Style="{DynamicResource Typography.Caption}" Text="{loc:Translate Help.InApp.Future}" Margin="0,8,0,0" />
            <Button Style="{DynamicResource LinkButton}" Content="{loc:Translate Settings.Debug.OpenLogs}" Command="{Binding OpenLogsCommand}" HorizontalAlignment="Left" Margin="0,16,0,0" />
        </StackPanel>
    </pages:PageChrome>
</UserControl>
