<UserControl
    x:Class="Viora.UI.Pages.Feedback.FeedbackPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:loc="clr-namespace:Viora.UI.Localization"
    xmlns:pages="clr-namespace:Viora.UI.Pages">
    <pages:PageChrome TitleKey="Feedback.Title" SubtitleKey="Feedback.Subtitle">
        <StackPanel MaxWidth="760">
            <Grid Margin="0,8,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Command="{Binding OpenBugCommand}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Card" Background="{DynamicResource Color.Surface}" BorderBrush="{DynamicResource Color.Border}" BorderThickness="1" CornerRadius="10" Padding="20" Cursor="Hand">
                                <StackPanel>
                                    <TextBlock Text="&#xE9F5;" FontFamily="Segoe MDL2 Assets" FontSize="20" Foreground="{DynamicResource Color.Danger}" />
                                    <TextBlock Style="{DynamicResource Typography.Heading}" FontSize="14" Text="{loc:Translate Feedback.Bug}" Margin="0,10,0,4" />
                                    <TextBlock Style="{DynamicResource Typography.BodySecondary}" TextWrapping="Wrap" Text="{loc:Translate Feedback.Bug.Description}" />
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Card" Property="Background" Value="{DynamicResource Color.SurfaceElevated}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <Button Grid.Column="2" Command="{Binding OpenFeatureCommand}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Card" Background="{DynamicResource Color.Surface}" BorderBrush="{DynamicResource Color.Border}" BorderThickness="1" CornerRadius="10" Padding="20" Cursor="Hand">
                                <StackPanel>
                                    <TextBlock Text="&#xE718;" FontFamily="Segoe MDL2 Assets" FontSize="20" Foreground="{DynamicResource Color.Success}" />
                                    <TextBlock Style="{DynamicResource Typography.Heading}" FontSize="14" Text="{loc:Translate Feedback.Feature}" Margin="0,10,0,4" />
                                    <TextBlock Style="{DynamicResource Typography.BodySecondary}" TextWrapping="Wrap" Text="{loc:Translate Feedback.Feature.Description}" />
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Card" Property="Background" Value="{DynamicResource Color.SurfaceElevated}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <Button Grid.Column="4" Command="{Binding OpenGeneralCommand}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Card" Background="{DynamicResource Color.Surface}" BorderBrush="{DynamicResource Color.Border}" BorderThickness="1" CornerRadius="10" Padding="20" Cursor="Hand">
                                <StackPanel>
                                    <TextBlock Text="&#xE77B;" FontFamily="Segoe MDL2 Assets" FontSize="20" Foreground="{DynamicResource Color.Accent}" />
                                    <TextBlock Style="{DynamicResource Typography.Heading}" FontSize="14" Text="{loc:Translate Feedback.General}" Margin="0,10,0,4" />
                                    <TextBlock Style="{DynamicResource Typography.BodySecondary}" TextWrapping="Wrap" Text="{loc:Translate Feedback.General.Description}" />
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Card" Property="Background" Value="{DynamicResource Color.SurfaceElevated}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
            <TextBlock Style="{DynamicResource Typography.Caption}" Text="{loc:Translate Feedback.OpenIssues}" Margin="0,12,0,0" />
            <TextBlock Style="{DynamicResource Typography.Caption}" Text="{loc:Translate Feedback.IncludeLogs}" Margin="0,4,0,0" TextWrapping="Wrap" />
        </StackPanel>
    </pages:PageChrome>
</UserControl>
